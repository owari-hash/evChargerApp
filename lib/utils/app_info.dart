/// Marketing version shown in the UI.
///
/// Keep in step with `version:` in pubspec.yaml, which is what feeds
/// CFBundleShortVersionString on iOS and versionName on Android.
const String kAppVersion = '1.0.1';
